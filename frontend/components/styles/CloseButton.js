import styled from 'styled-components';

const CloseButton = styled.button`
  background: black;
  color: white;
  font-size: 2rem;
  border: 0;
  position: absolute;
  z-index: 2;
  line-height: 2rem;
  right: 20px;
  cursor: pointer;
`;

export default CloseButton;
